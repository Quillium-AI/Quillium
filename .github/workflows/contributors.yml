name: Update Contributors

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update-contributors:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write  # Required for PR creation [[8]]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 16

      - name: Install all-contributors-cli (without saving to package.json)
        run: npm install --no-save all-contributors-cli@6.18.0  # <-- Added --no-save flag [[4]]

      - name: Update contributors
        run: npx all-contributors generate
        env:
          GITHUB_TOKEN: ${{ secrets.PAT }}

      # Create PR without npm artifacts
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.PAT }}
          commit-message: "docs(contributors): update list"
          title: "Update contributors list"
          body: "Automated contributors update by GitHub Actions"
          branch: update-contributors
          base: main
          author: github-actions <github-actions@github.com>
